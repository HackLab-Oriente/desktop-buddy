cmake_minimum_required(VERSION 3.16)

# Buddy Zero — framework seed + PoC testbed.
# Targets: esp32 (DevKit V1 PoCs today), esp32s3 (the real buddy).
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(buddy)
