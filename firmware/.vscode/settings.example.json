{
  "//": "Copy to firmware/.vscode/settings.json (gitignored). idf.espIdfPath and idf.port are per-machine; the ESP-IDF extension fills them in for you via its setup wizard.",

  "clangd.arguments": [
    "--background-index",
    "--query-driver=**",
    "--compile-commands-dir=${workspaceFolder}/build"
  ],

  "//target": "esp32s3 is the only supported target. The classic ESP32 was retired in #80.",
  "idf.customExtraVars": { "IDF_TARGET": "esp32s3" },
  "idf.flashType": "UART",
  "idf.openOcdConfigs": ["board/esp32s3-builtin.cfg"]
}
